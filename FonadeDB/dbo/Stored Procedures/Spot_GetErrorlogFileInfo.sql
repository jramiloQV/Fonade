CREATE PROCEDURE [dbo].[Spot_GetErrorlogFileInfo]
@FileSize DECIMAL (13) OUTPUT, @StartDate DATETIME OUTPUT, @EndDate DATETIME OUTPUT, @LogNumber INT, @LogFileName VARCHAR (2000) OUTPUT, @Print CHAR (1), @Debug CHAR (1), @LogType VARCHAR (20)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


